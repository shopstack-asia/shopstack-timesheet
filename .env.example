# ============================================
# Server Configuration
# ============================================
# Port สำหรับรัน development server (default: 3000)
PORT=3000

# ============================================
# NextAuth Configuration (จำเป็น)
# ============================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here

# Optional: Base URL สำหรับ Slack message links (ถ้าไม่ตั้งค่า จะใช้ NEXTAUTH_URL หรือ request headers แทน)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# or
# APP_URL=http://localhost:3000

# ============================================
# Google OAuth (จำเป็น)
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# Google Sheets API (จำเป็น)
# ============================================
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ============================================
# Zoho People API (จำเป็น)
# ============================================
ZOHO_CLIENT_ID=your-zoho-client-id
ZOHO_CLIENT_SECRET=your-zoho-client-secret
ZOHO_REFRESH_TOKEN=your-zoho-refresh-token
ZOHO_API_DOMAIN=https://people.zoho.com

# ============================================
# Slack Integration (ไม่บังคับ)
# ============================================
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Single Channel (แบบเดิม):
SLACK_CHANNEL_ID=your-slack-channel-id

# Multiple Channels (รองรับหลายช่องทาง, คั่นด้วย comma):
# SLACK_CHANNEL_IDS=C1234567890,C9876543210,C1111111111

# ============================================
# Email Configuration (ไม่บังคับ)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@shopstack.asia

# ============================================
# Cron Job Secret (จำเป็น)
# ============================================
CRON_SECRET=your-cron-secret-here
